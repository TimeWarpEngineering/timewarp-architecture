<Project Sdk="Microsoft.NET.Sdk">

  <!-- Shared substrate: the golden Ledger aggregate + EF state-store + integration-event seam,
       written ONCE and consumed by BOTH the Akka and Orleans hosts. Deliberately ZERO actor/grain
       references (grep this project for "Akka"/"Orleans" => nothing) so substrate-agnosticism is a
       structural fact, not a claim. -->

  <ItemGroup>
    <PackageReference Include="FluentValidation" />
    <PackageReference Include="Microsoft.EntityFrameworkCore" />
    <PackageReference Include="Npgsql.EntityFrameworkCore.PostgreSQL" />
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" />
  </ItemGroup>

  <ItemGroup Label="Golden pattern via source (NOT lagging packages) — read-only to the spike">
    <ProjectReference Include="..\..\..\source\foundation\foundation-domain\foundation-domain.csproj" />
    <ProjectReference Include="..\..\..\source\foundation\foundation-application\foundation-application.csproj" />
  </ItemGroup>

</Project>
