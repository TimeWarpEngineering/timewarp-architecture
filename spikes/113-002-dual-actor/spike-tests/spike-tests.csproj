<Project Sdk="Microsoft.NET.Sdk">

  <!-- The REAL template test stack (Fixie + Shouldly + TimeWarp.Fixie) is the evaluation criterion:
       "can a spike test drive the actor/grain without cluster scaffolding?" is answered with the
       tooling a template consumer actually has. -->

  <ItemGroup>
    <PackageReference Include="Fixie.TestAdapter" />
    <PackageReference Include="Shouldly" />
    <PackageReference Include="TimeWarp.Fixie" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\ledger-substrate\ledger-substrate.csproj" />
    <ProjectReference Include="..\akka-host\akka-host.csproj" />
    <!-- orleans-host reference added in its phase. -->
  </ItemGroup>

</Project>
