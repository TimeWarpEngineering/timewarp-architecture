paths:
  skills: skills
  evals: skills

suites:
  ci-gate:
    description: "PR gate — p0 + p1 priority stimuli"
    filter:
      priority: [p0, p1]
  nightly:
    description: "Weekly comprehensive run — p0, p1, p2"
    filter:
      priority: [p0, p1, p2]