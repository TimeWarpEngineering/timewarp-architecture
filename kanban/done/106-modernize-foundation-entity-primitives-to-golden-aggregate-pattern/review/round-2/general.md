# Round 2 — general
**Date:** 2026-07-19
**Scope reviewed:** commit 848549fa ("fix(foundation): apply 106 round-1 review findings M1-M10") vs 437f0e17; re-verification of round-1 findings M1–M10 plus scan of the fix delta

## Summary
All ten round-1 findings are genuinely fixed — each fix was verified against the post-fix code, and the two riskiest (M1 Version increment, M2/M3/M5 analyzer/guard alignment) were hard-verified in detail below. All four affected test projects run green (foundation-application-tests 13/13, foundation-domain-tests 34/34, web-domain-tests 26/26, analyzers-tests 74/74 including the 12 aggregate-invariants cases). The fix delta introduces no bugs; the scan surfaced two suggestion-level follow-ups (an unguarded `entry.Property("Version")` assumption in the increment path, and a new analyzer/guard asymmetry the M5 base-walk created around base-declared validators) and one nit (an untested new analyzer branch).

## Prior findings verification

- **M1 (bug, Version inert) — fixed, confirmed.** Hard-verified: (a) `IncrementModifiedVersions` (postgres-db-context.cs:91-100) sourcing from `PropertyEntry.OriginalValue` is the *correct* choice, not just acceptable — it matches the value `.IsConcurrencyToken()` puts in the UPDATE WHERE clause, and it makes the hook idempotent under execution-strategy retries (a failed `base.SaveChanges` leaves CurrentValue incremented; re-entry recomputes `Next(original)` to the same value instead of double-incrementing, which CurrentValue-based increment would have done). (b) Repeat saves in one context lifetime are correct: the default `acceptAllChangesOnSuccess: true` path runs `AcceptAllChanges` after save, refreshing OriginalValue to the saved value, so successive saves produce 1, 2, 3…; with `acceptAllChangesOnSuccess: false` the second save's stale WHERE fails with `DbUpdateConcurrencyException`, which is that overload's documented general contract, not a hook defect. (c) The funnel is still complete — only `SaveChanges(bool)` / `SaveChangesAsync(bool, ct)` are overridden and the parameterless base overloads delegate to them virtually; neither calls the other, so no double increment. (d) The `OnModelCreating` pin correctly covers every `IAggregateRoot` entity type present in the model (DbSet-discovered types are in the model before the method body runs); types a consumer might register after the loop would miss the pin, but the Design region explicitly frames the pin as defense-in-depth over EF's reflection-based private-setter access, so nothing is incorrect. (e) `EntityVersion.Next` is unit-tested; the honest "no live EF round-trip test" gap is recorded in entity-version.cs's Design region. entity.cs's rewritten Design region now describes the real two-party mechanism, including the failure mode when the host skips `.IsConcurrencyToken()`. See M11 for one residual robustness gap in the increment path.
- **M2 (bug, analyzer/runtime shape drift) — fixed, confirmed.** `ValidatesAggregate` now requires `!candidate.IsAbstract` (line 124), a parameterless ctor of any accessibility (line 125, with correct synthesized-default-ctor handling via `InstanceConstructors`), and `ContainingNamespace?.ToDisplayString() == "FluentValidation"` (line 132). No new false positives found: implicit parameterless ctors and zero-parameter primary constructors both appear in `InstanceConstructors` with `Parameters.Length == 0` and pass; a parameterized primary-ctor validator is rejected by the analyzer and also fails at runtime (Activator does not fill optional/required params), so the two sides agree in outcome. Guard wraps `MissingMethodException`/`MemberAccessException` into the new `MissingInvariantsValidatorException(Type, Exception)` overload while deliberately letting `TargetInvocationException` (genuine ctor bug) propagate — reasonable and documented. The remaining generic-aggregate gap is now honestly documented on both sides. New tests `Given_Abstract_Nested_Validator_StillFlags_Missing`, `Given_WrongNamespace_AbstractValidator_StillFlags_Missing`, `Throws_..._targets_a_different_type`, `Wraps_constructor_failure_...` all exist and pass.
- **M3 (suggestion, TWA0012 first-match) — fixed, confirmed.** `FindInvariantsValidators` returns all qualifying nested types; `AnalyzeNamedType` reports TWA0012 on each non-private one (lines 104-111); `Given_Two_Public_Nested_Validators_Flags_Both` passes. Guard's `SelectValidatorType` prefers `IsNestedPrivate` deterministically, documented as best-effort; the preference test is well-designed (asymmetric rules make the outcome prove which validator ran).
- **M4 (suggestion, MaxDisplayNameLength drift) — fixed, confirmed.** `EnsureDisplayNameLength` called from both `Create` (profile.cs:56) and `Rename` (line 64), referencing the shared const; Design region updated; at-max/over-max tests added and passing.
- **M5 (suggestion, proxy/base-type discovery) — fixed, confirmed.** `DiscoverAndInstantiateValidator` walks `BaseType` to null (terminates at `object`); `GetNestedTypes(Public | NonPublic)` does return private nested types of base classes (verified by the passing `Finds_a_validator_declared_on_a_base_type` test, whose fixture base declares a *private* Invariants); the contravariance reasoning (`IValidator<in T>` makes a base-targeted validator assignable to `IValidator<Derived>`) is correct and empirically pinned by `Runs_the_base_type_validator_against_the_subclass_instance`. See M12 for a new asymmetry this creates with TWA0011.
- **M6 (suggestion, child-change boundary) — fixed, confirmed.** "Known boundary" paragraph in postgres-db-context.cs Design region (lines 34-39) records the limitation, the intended navigation-metadata resolution, and correctly extends it to the Version increment as well.
- **M7 (nit, exception message) — fixed, confirmed.** Message no longer claims `IAggregateRoot`; points at the Profile exemplar + overview.md; Purpose/Design regions record the reasoning.
- **M8 (nit, "never a raw Guid" claim) — fixed, confirmed.** Reworded as convention, acknowledges `Entity<Guid>` compiles and the tests use it, names the analyzer as future enforcement.
- **M9 (nit, TWA0012 message overstatement) — fixed, confirmed.** messageFormat now says "can be picked up by assembly scanning".
- **M10 (suggestion, missing test cases) — fixed, confirmed.** All five claimed cases exist (base-class root, extended-interface root, same-named non-validator, internal validator, wrong-T guard case) and pass.

## Issues

### M11 — Severity: suggestion
- File: source/container-apps/web/web-infrastructure/persistence/postgres-db-context.cs:97-98
- Description: `IncrementModifiedVersions` calls `entry.Property(VersionPropertyName)` and casts `(long)versionProperty.OriginalValue!` unconditionally for every Modified `IAggregateRoot` entry. Nothing enforces that an `IAggregateRoot` also inherits `Entity<TId>` — the marker is a standalone interface (TWA0011 checks the validator, not the base class) — so a consumer aggregate implementing `IAggregateRoot` without a mapped `long Version` property (no `Entity<TId>` base, or `Version` ignored in mapping, or retyped) makes every save throw EF's generic `InvalidOperationException` ("The property 'Version' ... was not found") or an `InvalidCastException`, with no pointer at the convention. Notably `OnModelCreating` defends its own pin with a symmetric skip (`GetProperty(...) is null → continue`, line 77) — the increment path has no such guard, so the two halves of the same file disagree about whether Version-less roots are tolerated. Latent today (the context is entity-free and the exemplar inherits `Entity<TId>`), and it fails loud rather than silently, hence suggestion not bug.
- Suggestion: In `IncrementModifiedVersions`, skip entries where `entry.Metadata.FindProperty(VersionPropertyName)` is null (mirroring the OnModelCreating guard) — or, if a Version-less aggregate root should be a hard error, throw a convention-pointing message instead of EF's generic one. One Design-region sentence stating which choice was made.
- Status: open

### M12 — Severity: suggestion
- File: source/foundation/foundation-application/services/domain-invariants-guard.cs:13-16 vs source/analyzers/timewarp-architecture-convention-analyzers/aggregate-invariants-analyzer.cs:129-132
- Description: The M5 fix introduced a new analyzer/guard asymmetry the Design regions don't reconcile. The guard's Design region names "a validator declared on an abstract aggregate base rather than every concrete leaf" as one of the two real shapes the BaseType walk exists to support — and the passing `WidgetBaseWithValidator`/`WidgetSubclassWithoutOwnValidator` tests prove the guard supports it. But TWA0011 still requires every concrete leaf to declare its *own* nested validator with type argument equal to the *leaf* type (`ValidatesAggregate` matches `SymbolEqualityComparer.Equals(typeArg, aggregateType)` against directly nested members only), so the base-declared shape cannot build warning-free under the repo's warnings-as-errors policy. Net effect: of the two shapes the walk supports, only the EF-proxy case (proxy types are runtime-generated and never seen by the analyzer) is actually usable; the base-declared-validator case is runtime-supported but build-blocked, and the analyzer's own Design region ("concrete leaves are still checked") doesn't mention the contradiction with the guard's advertised capability. Safe direction (build-time friction, never silent corruption), hence suggestion.
- Suggestion: Either (a) teach `ValidatesAggregate` to also accept a nested validator inherited from a base class whose type argument is that base type (mirroring the guard's contravariance walk), with a test; or (b) declare in BOTH Design regions that base-declared validators exist for proxy support only and TWA0011 deliberately still demands a per-leaf declaration — so the disagreement becomes a recorded decision instead of an implicit contradiction.
- Status: open

### M13 — Severity: nit
- File: tests/analyzers/timewarp-architecture-analyzers-tests/aggregate-invariants-analyzer-tests.cs:169-215
- Description: Of the three new `ValidatesAggregate` conditions added for M2, two are pinned by tests (abstract nested validator, wrong-namespace `AbstractValidator`) but the third — `HasParameterlessConstructor` — has no analyzer test: an aggregate whose only nested validator has exclusively parameterized constructors should raise TWA0011, and no test exercises that branch (the equivalent runtime path *is* tested via `Wraps_constructor_failure_as_MissingInvariantsValidatorException`). The comment above `HasParameterlessConstructor` also makes a specific claim about synthesized default ctors appearing in `InstanceConstructors` that a test would pin cheaply.
- Suggestion: Add `Given_CtorParameterized_Nested_Validator_StillFlags_Missing` (validator with only `Invariants(string x)`) expecting TWA0011.
- Status: open
