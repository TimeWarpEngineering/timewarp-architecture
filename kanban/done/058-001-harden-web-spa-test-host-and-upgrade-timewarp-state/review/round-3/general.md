# Round 3 — general
**Date:** 2026-09-23
**Scope reviewed:** commits 4a34830b and c0b72263 (b494fcf4..HEAD, product files only)

## Summary

The two commits add an HTTP health check to the `yarp` ingress resource (gated `#if web`, since only web provides the catch-all route that "/" resolves to) and gate the SPA test host's mock-auth/API-client block behind `#if(api)` so a `SmokeNoApi` generated app compiles. Both changes are small, surgical, and match their stated rationale — decompiling `Aspire.Hosting`/`Aspire.Hosting.Yarp` 13.5.4 confirms the API signatures, defaults, and the "AddYarp registers no health check" claim; grepping the AppHost and test tree confirms no `WaitFor(yarp)` edge exists that the new check could deadlock; and grepping the whole `web-spa-integration-tests` project confirms every other file that references the now-gated types/usings is already excluded by `template.json` when `api` is off. No issues found.

## Verification notes

- **Item 1 (placement/routing):** `WithHttpHealthCheck(endpointName: "http")` sits inside `#if yarp` → `#if web` (`program.cs:199`–`230`, health check at `:229`), i.e. exactly the block that also declares the catch-all `yarpConfiguration.AddRoute(webServerHttp)` at `:263` (no path pattern = default `/`). With `web` excluded there is no catch-all and the comment says so ("Web-gated: with web excluded there is no catch-all, so `/` would 404 forever") — verified true by reading the full `#if web`/`#if api` route blocks (`:233`–`:266`): only `#if api` adds `/api/{**catch-all}`, nothing else answers bare `/`.
- **Item 2 (deadlock/ordering):** `grep -rn "WaitFor\b" source/ tests/` shows zero `.WaitFor(yarp)` / `.WaitFor()` edges pointing at the yarp/ingress resource anywhere in the builder graph — the only builder-graph `WaitFor` calls target `postgresDb`. Test code (`base-test.cs:60-64`, `ingress-smoke-tests.cs:53-55`) always waits `web-server` (and `api-server` when present) healthy *before* waiting on `ingress`, so the new health check (which itself round-trips through web-server) cannot introduce a new circular or blocking wait — it can only make the existing `ingress` wait take slightly longer and be trustworthy for the first time.
- **Item 3 (API compatibility):** Decompiled `Aspire.Hosting.Yarp.YarpResource` (13.5.4): implements `IResourceWithEndpoints`, so it satisfies `WithHttpHealthCheck<T>(...) where T : IResourceWithEndpoints`. Decompiled `AddYarp`: unconditionally calls `WithHttpEndpoint(..., "http", ...)`, so an endpoint literally named `"http"` always exists regardless of `Ingress:HttpPort` config — `endpointName: "http"` always resolves. Decompiled `WithHttpHealthCheck` overload confirms defaults: `path` defaults to `"/"`, `statusCode` defaults to `200` — matches the comment's claims exactly.
- **Item 4 (api-off compile safety):** `git grep` for `IConfiguration|ConfigurationBuilder|IAccessTokenProvider|IOptions<|IApiServerApiService|ApiServerApiService` across `tests/container-apps/web/web-spa-integration-tests` found three files: `aspire-spa-test-application.cs` (fixed by this diff), `serialization/api-service-body-casing-tests.cs`, and `features/weather-forecast/weather-forecast-state-fetch-weather-forecasts-action-tests.cs`. Both of the latter are excluded outright by `.template.config/template.json`'s `(!api)` modifier (`tests/container-apps/web/web-spa-integration-tests/serialization/api-service-body-casing-tests.cs` and `tests/container-apps/web/web-spa-integration-tests/features/weather-forecast/**`), so they never reach the compiler in a `SmokeNoApi` generated app. No orphaned reference to a now-gated using remains.
- **Item 5 (TWA0008):** `git diff ... | grep -nE '^\+.*(#if|#endif)'` matches only the two real preprocessor directives added (`#if(api)` / `#endif` in `aspire-spa-test-application.cs`); none of the new prose comments contain a literal `#if`/`#endif` token — they use paraphrases ("the api conditional", "Web-gated", "excluded when api is off").
- **Item 6 (Design region accuracy):** `ingress-smoke-tests.cs`'s updated Design comment says the hand-rolled `WaitForIngressReachableAsync` poll is now "a backstop" that "returns on its first attempt". Read the implementation (`:103-132`): it does a single `GET "/"` on the same `"ingress"`/`"http"` client and endpoint the new health check probes, returning on any response and only looping on `HttpRequestException`. Since the preceding `WaitForResourceHealthyAsync("ingress")` already proved a 200 at `/`, the first `GetAsync` call will succeed — the "first attempt" claim holds. `base-test.cs`'s new comment is consistent with this and with task 155's existing wait-edge policy ("Do NOT add a retry/poll here").
- **Item 7 (style):** 2-space indentation, no underscores, comment style matches the file's existing long-form Design-region prose. No deviations found in the diff.
- Also checked: no other resource in `source/` or `tests/` already uses `WithHttpHealthCheck`/`WithHealthCheck` (this is the first usage in the repo — no established convention it deviates from). The AppHost project only references `Aspire.Hosting.Yarp`/`Aspire.Hosting.PostgreSQL`/`Aspire.Hosting.EntityFrameworkCore` (no Kubernetes/ACA publish packages), so the comment's "publish mode emits a manifest and never executes [health checks]" claim is accurate for this repo's actual publish target.

## Issues

No issues raised.
