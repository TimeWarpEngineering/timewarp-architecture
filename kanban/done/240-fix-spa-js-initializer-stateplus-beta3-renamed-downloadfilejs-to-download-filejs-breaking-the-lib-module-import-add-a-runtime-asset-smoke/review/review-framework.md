# Review framework — task 240

**Date:** 2026-09-19
**Host task:** kanban/in-progress/240-fix-spa-js-initializer-stateplus-beta3-renamed-downloadfilejs-to-download-filejs-breaking-the-lib-module-import-add-a-runtime-asset-smoke/
**Diff scope:** branch `task/240-fix-spa-js-initializer-stateplus-beta3-renamed-dow` vs `origin/master` (product commits `9ce03f87` + kanban results `40d7c50c`)
**Plan / brief:** Fix SPA JS initializer static `_content` imports after TimeWarp.State / State.Plus 12.0.0-beta.3 kebab-case rename (`downloadFile.js` → `download-file.js`, plus `logger.js` / `constants.js` / `timewarp-state.js`); add a runtime import-graph smoke and a host-free `dev template-smoke` NuGet-cache check.
**Effort:** 1 (general only)
**Reviewer roster:** general
**Session IDs:** review oracle grok session `01a0b917-dc84-7c33-9376-768e42d7aa6a` (2026-09-19)

## Ground rules

- Reviewers are read-only on product code; they write only under `review/round-1/`
- Severity: bug | suggestion | nit — Status starts as open
- Do not invent issues to fill space; zero issues is a valid outcome
- Address the diff and surrounding call sites; re-verify falsifiable claims against the repo
- Prior rounds are immutable; new work goes in `round-(N+1)/`

## Brief for general

Live after task 237: `GET /_content/TimeWarp.State.Plus/js/downloadFile.js` 404 rejected the whole `web.spa.*.lib.module.js` initializer (`window.Spa` never assigned). Same kebab-case drop also renamed TimeWarp.State `Logger.js` / `Constants.js` / `TimeWarpState.js`.

Requirements to verify:

1. **Import fix** — `web.spa.lib.module.ts` uses kebab-case `_content` paths; ambient `.d.ts` module names match; `java-script-interop-constants.cs` comment matches. TS emit (`wwwroot/js/web.spa.lib.module.js`, gitignored) is regenerated by the build.
2. **Runtime asset smoke** — `web-server-integration-tests` loads `/web-server.modules.json` (or `Blazor-Web-Initializers` fallback), GETs each initializer, parses static `import` specifiers, asserts root-relative targets return 200 + JavaScript content type, recurse one level.
3. **Template-smoke** — host-free `_content/<pkg>/...` → NuGet cache `staticwebassets/` after each generated-app build, or documented follow-up.
4. **Live verify** — Results may note recycle of `dev run` / `arch.timewarp.work` after merge; do not treat an un-recycled public share as a product defect.

Falsifiable claims to re-verify:

- No remaining PascalCase `_content` specifiers in SPA TS / d.ts (`downloadFile.js`, `Logger.js`, `Constants.js`, `TimeWarpState.js`, `ReduxDevTools.js`).
- Emit assertion uses ordinal/case-sensitive compares (Shouldly default is case-insensitive).
- HTTP graph smoke actually fetches initializer + one level of root-relative imports.
- Template-smoke walks generated `web.spa.lib.module.js` against `Directory.Packages.props` versions and `{NuGet cache}/<id>/<version>/staticwebassets/...`.
- Relative `./spa.js` skip is by brief (root-relative only); Counter's `timewarp-state.js` is still updated.
- `dev build` 0/0 and JsInitializer tests are claimed green in Results — spot-check the tests compile and match existing HostGraph / task-200 patterns.

Finding template: `review/round-1/general.md` (see tw-implementation-review).
