# Round 1 — leftovers
**Date:** 2026-09-09
**Scope reviewed:** Retired-framework residue (Fixie/xUnit/FluentAssertions/Tailwind/npm/postcss) in csproj/props/CPM/.gitignore/.editorconfig/CI/scripts; orphaned files (uncompiled sources, unreferenced .razor.css/.js, tools/fix-analyzer-debug.reg, BannedSymbols.txt, evals/spikes/samples/runfiles stub dirs, empty dirs); duplicate test suites (`foundation-domain-jaribu-tests` vs `foundation-domain-tests`); `scripts/*.ps1` vs task-061 dev-CLI migration; stale naming after the 132-001 SPA identity fold (`Features.Authentication`/`Features.Account`, old page/component names) across source/tests/documentation/skills; stale PascalCase path references (`Source/`, `Tests/`, `Web.Server`, `Web.Spa`, `Features/`) in code, docs, csproj, launchSettings, `.vscode`; git-tracked junk (`.claude/`, `.idea/`, `.local/`, binlogs, `dev.xml`).

## Summary
The two big migrations (Fixie/xUnit→Jaribu, Tailwind/npm→FluentUI+CSS) and the 132-001 identity fold are clean: no dead package references, no npm/postcss/tailwind config, no orphaned `Features.Authentication`/`Features.Account` namespaces or directories, and the SPA's `features/identity/` tree (including the `Authentication.razor` RemoteAuthenticatorView page, which is a standard ASP.NET naming convention, not a fold leftover) is coherent. Two genuine leftovers surfaced: a stale documentation page describing a lifecycle API that no longer exists at the path it links to (and names methods that don't match the current Jaribu convention), and an orphaned duplicate Jaribu runfile whose own comment describes a rationale (parallel Fixie/Jaribu example) that Fixie's full retirement (145-007) has since invalidated. `scripts/*.ps1` migration to the `dev` CLI (task 061) is still incomplete, which is expected since that task is still open — noted for completeness, not scored as a new bug.

## Issues

### Issue 1 — Severity: bug
- File: documentation/developer/how-to-guides/testing/how-to-add-lifecycles-to-tests.md:7
- Description: The doc says the testing convention supports `Setup`/`Cleanup` lifecycle method names and links to `../../../../Tests/TimeWarp.Architecture.Testing/ConventionTests/LifecycleExamples.cs`. That path uses the old PascalCase `Tests/` layout and the file does not exist anywhere in the repo (`find . -iname "*lifecycleexamples*"` returns nothing). It is also factually wrong for current Jaribu usage: the live convention (see `documentation/test-structure.md:11`, `skills/tw-feature-placement/references/co-located-jaribu-runfiles.md`) uses `SetupOnce`/`CleanUpOnce`, not `Setup`/`Cleanup`. This ships as documentation-of-record to every generated app.
- Suggestion: Rewrite the how-to to describe Jaribu's actual `SetupOnce`/`CleanUpOnce` (and `SessionFixture` C-share variant) lifecycle, linking to a real exemplar such as `source/container-apps/api/features/weather-forecast/get-weather-forecasts/get-weather-forecasts-tests.cs` or `tests/common/timewarp-testing/host-graph-factory.cs`, or fold its content into the `tw-jaribu`/`tw-feature-placement` skill docs and delete the stale how-to.
- Status: open

### Issue 2 — Severity: suggestion
- File: tests/foundation/foundation-domain-jaribu-tests/enumeration.cs:5-7
- Description: The file's own header comment says it is a "Jaribu duplicate of the Fixie suite in tests/foundation/foundation-domain-tests (task 046). Kept in parallel as a small worked example of BOTH test frameworks." That rationale is now stale: `tests/foundation/foundation-domain-tests/foundation-domain-tests.csproj` was itself mechanically converted from Fixie to Jaribu under task 145-007 (its csproj comment says exactly that), so there is no longer a Fixie suite to contrast against — this runfile now duplicates a same-framework sibling suite for no documented reason. It has no `.csproj` and is not referenced by any `.sln`/`.slnx`/`.props`/`.targets`/CI path (only historical mentions in `kanban/done/046-...md` and `kanban/done/134-spike-jaribu-co-located-integration-tests/*`), so it isn't part of any build or CI gate — it can silently bit-rot.
- Suggestion: Either delete `tests/foundation/foundation-domain-jaribu-tests/` (its content is superseded by the now-Jaribu `foundation-domain-tests`), or if it's intentionally kept as a "how to write a co-located Jaribu runfile" teaching exemplar, update its header comment to say so and cross-link it from `tw-jaribu`/`tw-feature-placement` skill docs so it doesn't read as forgotten migration debris.
- Status: open

### Issue 3 — Severity: suggestion
- File: scripts/ (13 files: build-dependency-diagram.ps1, profile.ps1, get-next-task-number.ps1, describe.ps1, git/*.ps1 ×3, postgres/*.ps1 ×5, windows/enable-long-paths.ps1)
- Description: AGENTS.md's "Definition of Done"/CI section implies scripting now lives in the `dev` CLI (task 061: "migrate remaining ps1 scripts to dev CLI endpoints", still open in `kanban/to-do/`). None of these `.ps1` files are referenced by `.github/workflows/workflow.yml`; they're only referenced from `scripts/overview.md` and the open task-061 checklist itself, confirming they're pending migration rather than already-dead.
- Suggestion: No action needed here beyond what task 061 already tracks — leaving this note so round-1 doesn't miss it, but it should not be scored as a fresh leftover since an open task already owns the cleanup.
- Status: open

## Checked clean
- Fixie/xUnit/FluentAssertions references in `*.csproj`/`*.props`/`*.targets`/`Directory.Packages.props`/`.editorconfig`/`.github/workflows/`/`scripts/`: none live — only correct historical comments (e.g. `tests/foundation/foundation-domain-tests/foundation-domain-tests.csproj:3`, `tests/common/timewarp-testing/timewarp-testing.csproj:7`) documenting the 145-007 conversion.
- Tailwind/postcss/npm/node_modules/package.json/package-lock.json: none present. `web-spa.csproj:119` correctly notes the npm lint/format toolchain was removed under 059-006; `tsconfig.json` under `web-spa` is live (compiles `source/spa.ts` etc. via `Microsoft.TypeScript.MSBuild`), not migration debris.
- `.gitignore`/`.editorconfig`: no stale Fixie/xUnit/Tailwind-specific entries found.
- `tools/fix-analyzer-debug.reg`: live one-time VS debugging utility, tracked deliberately per `kanban/done/067-...md`.
- `BannedSymbols.txt`: wired into `Directory.Build.props` and the template packaging csproj — live.
- `evals/`, `runfiles/`, `spikes/`, `samples/`: intentional placeholders (readme/overview.md/.gitkeep), not orphaned migration artifacts.
- Empty directories: none found outside the review task's own freshly-created `review/round-1/` folder.
- Sampled `.razor.css` files under `web-spa`: all have a matching sibling `.razor`. Sampled `wwwroot/*.js` files (`spa.js`, `counter.js`, `web-authn.js`): all referenced from `.razor`/`.cs`.
- Post-132-001 fold: no `Features.Authentication` or `Features.Account` references anywhere in `source/`, `tests/`, `documentation/`, `skills/` (one `Features.Accounting` hit in docs is an unrelated example domain name). No leftover `authentication`/`account` directories under either `source/container-apps/web/features/` or `source/container-apps/web/projects/web-spa/features/` — only `identity/`. `features/identity/pages/Authentication.razor` is the standard ASP.NET `RemoteAuthenticatorView` callback page (namespaced `TimeWarp.Architecture.Features.Identity`), not fold debris.
- Stale PascalCase path references (`Source/`, `Tests/`, `Web.Server`, `Web.Spa`, `Features/` as folder paths): none in active source/config outside the one doc link captured as Issue 1; other `Source/` hits in `skills/` point at an external reference repo (legitimate); `Web.Server`/`Web.Spa` hits elsewhere are legitimate assembly/resource names, not stale folder paths.
- Git-tracked junk: no `.dbg`/`.binlog`/`dev.xml` tracked; `.claude/` is deliberately tracked (bundled skills); `.idea/` tracked via an intentional whitelist in `.gitignore`; `.local/` is untracked and not currently shown in `git status` (harmless, ignored by broader patterns); `.vscode/settings.json`'s pending diff is a Peacock color-customization change, unrelated to any migration.
