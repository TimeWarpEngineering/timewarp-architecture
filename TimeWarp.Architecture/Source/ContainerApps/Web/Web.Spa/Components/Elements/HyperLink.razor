@namespace TimeWarp.Architecture.Components
@inherits BaseComponent
@implements IParentComponent

@code {
  [Parameter] public RenderFragment ChildContent { get; set; } = null!;

  protected override void OnInitialized()
  {
    base.OnInitialized();
    BaseCssClass = "cursor-pointer underline text-gray-600 hover:text-gray-800 visited:text-purple-600";
  }

  // private CssBuilder CssBuilder =>
  //   new CssBuilder(BaseClass)
  //     .AddClasses(GetClassFromAttributes());
}

<NavLink class=@BaseCssBuilder @attributes=Attributes>
  @ChildContent
</NavLink>

