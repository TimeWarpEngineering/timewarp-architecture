@namespace TimeWarp.Architecture.Pages

@inherits BaseComponent

@code{
  public static string Title => "Event Stream";
  public static readonly RenderFragment NavLink =
    @<FluentNavLink Icon=@(new Icons.Regular.Size20.Question()) Href=@GetPageUrl() Match=NavLinkMatch.All>@Title</FluentNavLink>;
}

<PageTitle>Event Stream Page</PageTitle>

<StackedPage>
  <HeaderContent>Event Stream Page</HeaderContent>
  <MainContent>
    <SimpleAlert Title="Alert Title" class="mt-2">EventStream is an example of middleware.  It adds each action to a list.</SimpleAlert>
    <EventStream />
  </MainContent>
</StackedPage>
