trigger:
 branches:
  include:
  - master
 paths:
  include:
  - Source/TimeWarp.Architecture.Template/*

pr:
 branches:
   include:
     - master
 paths:
  include:
  - Source/TimeWarp.Architecture.Template/*


pool:
  name: TimeWarpEnterprises

variables:
 Major: '5'
 Minor: '0'
 MajorAndMinor: '$(Major).$(Minor)'
 Patch: $[counter(variables.MajorAndMinor,0)]
 DotNetSdkVersion: 5.0.301
 Version: '$(Major).$(Minor).$(Patch)+$(DotNetSdkVersion)'
 Configuration: debug

steps:
- script: dotnet --version
- script: echo Version $(Version)

- task: DotNetCoreCLI@2
  displayName: Build Template projects
  inputs:
    command: build
    projects: 'Source/TimeWarp.Architecture.Template/**/*.csproj'
    configuration: $(Configuration)

# - task: DotNetCoreCLI@2
#   displayName: Test
#   inputs:
#    command: test
#    projects: 'Source/TimeWarp.Architecture.Template/**/*Integration.Tests/*.csproj'
#    configurationToPack: $(Configuration)

- task: DotNetCoreCLI@2
  displayName: Pack into Nuget
  inputs:
   command: pack
   packagesToPack: $(Build.SourcesDirectory)/Source/TimeWarp.Architecture.Template/TimeWarp.AspNetCore.Blazor.Templates.csproj
   configurationToPack: $(Configuration)
   versioningScheme: byEnvVar
   versionEnvVar: Version

- task: PublishBuildArtifacts@1
  displayName: Publish Artifact
  inputs:
    PathtoPublish: '$(Build.ArtifactStagingDirectory)'
    ArtifactName: drop
    publishLocation: Container
